{
  "name": "line-order-system",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/api",
    "apps/customer-web",
    "apps/admin-web"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspace @line-order/api",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
